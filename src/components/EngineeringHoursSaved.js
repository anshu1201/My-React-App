// src/components/EngineeringHoursSaved.js
import { Box, Typography } from '@mui/material';

export const EngineeringHoursSaved = () => (
  <Box>
    <Typography variant="h6">Engineering Hours Saved</Typography>
    <Typography variant="caption">The total number of candidates that have been in 
    the hiring pipeline</Typography>
    <Typography variant="h4">600 Hours</Typography>
  </Box>
);
