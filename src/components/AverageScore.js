// src/components/AverageScore.js
import { Box, Typography } from '@mui/material';

export const AverageScore = () => (
  <Box>
    <Typography variant="h6">Average Score</Typography>
    <Typography variant="caption">The average of all the students that have taken the assessment thus far.</Typography>
    <Typography variant="h4">66</Typography>
  </Box>
);
