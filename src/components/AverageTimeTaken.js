// src/components/AverageTimeTaken.js
// src/components/AverageScore.js
import { Box, Typography } from '@mui/material';

export const AverageTimeTaken = () => (
  <Box>
    <Typography variant="h6">Average Time Taken Per 
    Assessment</Typography>
    <Typography variant="caption">The total number of candidates that have been in 
    the hiring pipeline.</Typography>
    <Typography variant="h4">6 Hours</Typography>
  </Box>
);

//export default AverageTimeTaken;
